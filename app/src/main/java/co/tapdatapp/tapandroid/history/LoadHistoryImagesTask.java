/**
 * async task to fetch images if necessary and set them on the view
 * for a history line item.
 */

package co.tapdatapp.tapandroid.history;

import android.os.AsyncTask;
import android.view.View;

public class LoadHistoryImagesTask
extends AsyncTask<View, Void, Void> {
    @Override
    protected Void doInBackground(View... views) {
        return null;
    }
}
