<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:textColor">@color/text_color</item>
        <!-- buttonStyle seems to break onClick() on pre-L, do it the hard
             way until I figure out what's going on -->
        <!--<item name="android:buttonStyle">@style/AppTheme.Button</item>-->
    </style>

    <style name="AppTheme.Button">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/button_selector</item>
        <item name="android:textColor">@color/button_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">3dp</item>
        <item name="android:textSize">24sp</item>
    </style>

    <style name="AppTheme.Load_Buttons">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/load_button_selector</item>
        <item name="android:textColor">@color/load_button_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">30sp</item>
    </style>

    <style name="AppTheme.Withdraw_Button">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/button_selector_withdraw</item>
        <item name="android:textColor">@color/button_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">3dp</item>
        <item name="android:textSize">24sp</item>
    </style>

    <style name="AppTheme.circular_button">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/button_circular_text</item>
        <item name="android:background">@drawable/button_circular</item>
    </style>

    <style name="AppTheme.header1">
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
